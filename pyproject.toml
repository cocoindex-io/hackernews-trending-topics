[project]
name = "hn-trending-topics"
version = "0.1.0"
description = "CocoIndex v1 example: scrape HackerNews and extract trending topics with an LLM."
requires-python = ">=3.11"
dependencies = [
    "cocoindex[postgres]>=1.0.7",
    "asyncpg>=0.29.0",
    "aiohttp>=3.9.0",
    "litellm>=1.40.0",
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.1",
]

[tool.setuptools]
packages = []
